{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Listen for XDebug",
        "type": "php",
        "request": "launch",
        "port": 9000,
        "pathMappings": {
          "/laravel8/laravel8": "./" // Dockerのドキュメントルート:ローカルのドキュメントルート
        }
      }
    ]
}
